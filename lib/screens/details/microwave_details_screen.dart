import 'package:flutter/material.dart';

class MicrowaveDetailsScreen extends StatelessWidget {
  const MicrowaveDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Fix Microwave')),
    );
  }
}
